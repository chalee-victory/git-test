# Python & Git 기초 과제 최종 진행 보고서
## 나만의 프롬프트 관리 프로그램

| | |
|---|---|
| **작성자** | 에펠 |
| **GitHub 저장소** | https://github.com/chalee-victory/git-test |
| **로컬 작업 폴더** | `C:\git-test` |
| **개발 환경** | Python 3.12.9 / Git 2.55.0.windows.3 / VS Code |
| **작성일** | 2026-08-21 |

---

## 목차

1. 과제 개요
2. 개발 환경 및 저장소 설정 (1~2단계)
3. 데이터 구조 및 메뉴 설계 (3단계)
4. 핵심 기능 구현 (4단계) — 함수별 상세 설명
5. 브랜치 생성 및 병합 실습
6. 예외 처리 (5단계)
7. README 작성 및 Git 명령어 완주 (6단계)
8. **보너스 과제 — JSON 데이터 영속화**
9. 실전 트러블슈팅 — Push 충돌과 병합 커밋 해결
10. 최종 커밋 히스토리
11. 요구사항 충족 현황 종합
12. 제출물 목록
13. 학습 성과 및 핵심 개념 총정리

---

## 1. 과제 개요

Python과 Git을 처음 배우는 학습자가 "프롬프트 관리 프로그램" 과제를 직접 설계·구현하며, 파이썬 기초 문법(변수, 조건문, 반복문, 리스트, 딕셔너리, 함수)과 Git/GitHub를 이용한 실무형 버전 관리 워크플로우(커밋, 브랜치, 병합, 원격 저장소 동기화)를 체득하는 것을 목표로 하였다.

**최종 산출물**
- 콘솔 기반 프롬프트 관리 프로그램 (`main.py`) — 필수 기능 7개 + 보너스 기능 1개
- GitHub 저장소 (`chalee-victory/git-test`) — 커밋 16개, 브랜치 병합 이력 포함

---

## 2. 개발 환경 및 저장소 설정 (1~2단계)

### 2-1. 개발 환경 점검

| 항목 | 결과 |
|---|---|
| Python 버전 | 3.12.9 |
| Git 버전 | 2.55.0.windows.3 |
| Git 사용자 이름 | 에펠 |
| Git 사용자 이메일 | chalee6110@gmail.com |
| 기본 브랜치명 | main |
| VSCode ↔ GitHub 연동 | `chalee-victory` 계정 연결 확인 완료 |

### 2-2. GitHub 저장소 + 로컬 연결

- **GitHub 저장소**: `chalee-victory/git-test`
- **로컬 작업 폴더**: `C:\git-test`
- **원격 연결(origin)**: `https://github.com/chalee-victory/git-test.git`

**진행 중 발생한 이슈와 해결 과정**

이 단계에서는 예상치 못한 세 가지 문제를 직접 해결하며 Git의 저장소 구조를 깊이 이해하는 계기가 되었다.

1. **문제**: 과거 홈 폴더(`C:\Users\OWNER`)에 실수로 `git init`을 실행하여 의도치 않은 저장소가 생성되어 있었음
   **해결**: `Remove-Item -Recurse -Force`로 `.git` 폴더 삭제 → `git status` 실행 시 `fatal: not a git repository` 확인으로 정상 해제 검증

2. **문제**: `C:\git-test` 폴더 내 과제와 무관한 예전 연습 파일/폴더가 혼재되어 있었음
   **해결**: `C:\old-practice-backup`으로 이동 보관 (삭제 대신 안전하게 격리)

3. **문제**: `main` 브랜치에 과제와 무관한 예전 커밋 4개가 남아있었고, 작업 위치도 `sub` 브랜치였음
   **해결 절차**:
   - `git checkout --orphan clean-main` — 커밋 이력이 전혀 없는 새 브랜치 생성
   - 불필요한 파일은 `git rm --cached`로 스테이징에서 제외 후 보관 폴더로 이동
   - `chore: init project with README` 커밋 생성
   - `git branch -D main` (기존 main 삭제) → `git branch -m main` (clean-main → main으로 개명)
   - `git push -u origin main --force`로 GitHub 원격 저장소까지 깨끗한 상태로 강제 반영

> 이 과정을 통해 "브랜치는 단순한 이름표가 아니라 커밋 이력을 가리키는 포인터"라는 점, 그리고 `--force` 푸시가 원격 저장소를 로컬 상태로 덮어쓰는 강력한 명령어임을 체감하였다.

---

## 3. 데이터 구조 및 메뉴 설계 (3단계)

### 3-1. 기본 데이터 구조

`prompts`라는 리스트 안에 프롬프트 하나당 딕셔너리 하나씩 저장하는 구조로 설계하였다.

```python
prompts = [
    {
        "title": "제목",
        "content": "프롬프트 내용",
        "category": "카테고리",
        "favorite": False
    },
    ...
]
```

기본 데이터로 4개(요구사항 "최소 3개 이상" 충족)를 등록하였으며, 이전 미션에서 실제로 사용했던 프롬프트(이미지 생성, 민원 챗봇 테스트 케이스 2종, 페르소나 정의)를 재활용하였다.

### 3-2. 메뉴 뼈대

```python
def show_menu():
    print("=== 나만의 프롬프트 관리 ===")
    print("1. 프롬프트 추가")
    ...
    print("0. 종료")

while True:
    show_menu()
    choice = input()
    if choice == "1":
        ...
    elif choice == "0":
        break
```

`while True` 무한 루프와 `if/elif` 분기 구조로, 사용자가 종료를 선택(`0`)하기 전까지 계속 메뉴로 복귀하는 구조를 완성하였다.

**커밋**: `dca795b`(init), `b23c829`(base data + menu skeleton)

---

## 4. 핵심 기능 구현 (4단계) — 함수별 상세 설명

7개 함수를 순서대로 구현하였으며, 각 함수가 완성·테스트될 때마다 개별 커밋을 남겼다.

### 4-1. `add_prompt(prompts)` — 프롬프트 추가

```python
def add_prompt(prompts):
    title = input("제목(Enter=>종료): ")
    if title == "":
        return
    content = input("내용: ")
    while content == "":
        content = input("내용: ")
    category = input("카테고리: ")
    while category == "":
        category = input("카테고리: ")
    chk = input("입력하시겠어요?(Y/N): ")
    if chk != 'Y':
        return
    new_prompt = {"title": title, "content": content, "category": category, "favorite": False}
    prompts.append(new_prompt)
```

**핵심 개념**: 제목은 빈 값 입력 시 즉시 취소(`return`)되도록, 내용/카테고리는 값이 채워질 때까지 재입력을 요구하는 `while` 루프로 구분 설계하였다. 또한 Y/N 확인 절차를 추가하여 실수로 인한 등록을 방지하였다(`chk != 'Y'` 조건으로, Y가 아닌 모든 입력을 취소로 처리하도록 최종 다듬음).

### 4-2. `show_list(prompts)` — 프롬프트 목록

```python
def show_list(prompts):
    if not prompts:
        print("등록된 프롬프트가 없습니다.")
        return
    for i, prompt in enumerate(prompts, start=1):
        star = "⭐" if prompt["favorite"] else ""
        print(f"{i}. [{prompt['category']}] {prompt['title']} {star}")
    print(f"총 {len(prompts)}개의 프롬프트")
```

**핵심 개념**: `enumerate(prompts, start=1)`을 사용해 리스트를 순회하며 번호(`i`)와 각 딕셔너리(`prompt`)를 동시에 얻는 방법을 학습하였다. 컴퓨터 내부 인덱스는 0부터 시작하지만, 사용자에게 보여줄 번호는 1부터 시작해야 자연스러우므로 `start=1`을 사용하였다. 조건부 표현식(`"⭐" if 조건 else ""`)으로 즐겨찾기 표시를 간결하게 처리하였다.

### 4-3. `show_by_category(prompts)` — 카테고리별 조회

```python
def show_by_category(prompts):
    for i, cat in enumerate(categories, start=1):
        print(f"{i}) {cat}")
    choice = input("선택: ")
    try:
        selected = categories[int(choice) - 1]
    except:
        print("잘못된 번호입니다.")
        return
    count = 0
    for i, prompt in enumerate(prompts, start=1):
        if prompt["category"] == selected:
            star = "⭐" if prompt["favorite"] else ""
            print(f"{i}. {prompt['title']} {star}")
            count += 1
    if count == 0:
        print("해당 프롬프트가 없습니다")
    else:
        print(f"총 {count}개의 프롬프트")
```

**핵심 개념**: 카테고리 목록(`categories`)은 요구사항에 따라 고정된 6개 항목으로 미리 정의하였다. `prompts`에서 실제 사용된 카테고리만 동적으로 추출하는 방식도 고려했으나, 아직 사용되지 않은 카테고리가 선택지에서 누락되는 문제와 오타로 인한 데이터 불일치 위험이 있어 고정 리스트 방식을 채택하였다.

### 4-4. `search_prompt(prompts)` — 프롬프트 검색

```python
def search_prompt(prompts):
    keyword = input("검색어: ")
    count = 0
    for i, prompt in enumerate(prompts, start=1):
        if keyword in prompt["title"] or keyword in prompt["content"]:
            star = "⭐" if prompt["favorite"] else ""
            print(f"{i}. [{prompt['category']}] {prompt['title']} {star}")
            count += 1
    if count == 0:
        print("검색 결과가 없습니다.")
    else:
        print(f"{count}개의 프롬프트를 찾았습니다.")
```

**핵심 개념**: `in` 연산자를 사용한 부분 문자열 포함 검사(`keyword in prompt["title"]`)를 적용하였다. 이는 완전 일치(`==`)가 아닌 "부분적으로 포함되어 있으면 참"이라는 점에서 `show_by_category()`의 필터링 로직과 차이가 있다. 제목 또는 내용 중 하나라도 일치하면 검색되도록 `or` 연산자로 두 조건을 결합하였다.

### 4-5. `show_detail(prompts)` — 프롬프트 상세 보기

```python
def show_detail(prompts):
    number = input("번호 입력: ")
    try:
        index = int(number) - 1
        prompt = prompts[index]
    except:
        print("잘못된 번호입니다.")
        return
    star = "⭐" if prompt["favorite"] else ""
    print(f"제목: {prompt['title']}")
    print(f"카테고리: {prompt['category']}")
    print(f"즐겨찾기: {star}")
    print("내용:")
    print(prompt["content"])
```

**핵심 개념**: 지금까지의 함수들이 리스트 전체를 순회(`for`)하며 조건을 검사한 것과 달리, 이 함수는 리스트 인덱싱(`prompts[index]`)으로 특정 항목 하나만 직접 접근하는 방식을 처음 사용하였다.

### 4-6. `toggle_favorite(prompts)` — 즐겨찾기 관리

```python
def toggle_favorite(prompts):
    number = input("프롬프트 번호 입력: ")
    try:
        index = int(number) - 1
        prompt = prompts[index]
    except:
        print("잘못된 번호입니다.")
        return
    prompt["favorite"] = not prompt["favorite"]
    if prompt["favorite"]:
        print(f"'{prompt['title']}' 프롬프트를 즐겨찾기에 추가했습니다!")
    else:
        print(f"'{prompt['title']}' 프롬프트를 즐겨찾기에서 해제했습니다.")
```

**핵심 개념 — mutable 자료형과 참조 전달**: `prompt = prompts[index]`로 얻은 `prompt`는 딕셔너리를 복사한 것이 아니라, 원본 딕셔너리를 가리키는 별도의 이름표에 불과하다. 따라서 `prompt["favorite"] = not prompt["favorite"]`로 값을 직접 수정하면, 원본 `prompts` 리스트 안의 데이터도 함께 변경된다. 이는 파이썬에서 리스트·딕셔너리가 "변경 가능(mutable)" 자료형이기 때문이며, 숫자·문자열 같은 "변경 불가능(immutable)" 자료형과 근본적으로 다른 동작 원리이다. 이 원리는 `show_list()`를 재호출하여 즐겨찾기 표시(⭐)가 실제로 반영되는지 확인하는 방식으로 검증하였다.

### 4-7. `show_favorites(prompts)` — 즐겨찾기 목록

```python
def show_favorites(prompts):
    count = 0
    for i, prompt in enumerate(prompts, start=1):
        if prompt["favorite"]:
            print(f"{i}. [{prompt['category']}] {prompt['title']} ⭐")
            count += 1
    if count == 0:
        print("즐겨찾기한 프롬프트가 없습니다.")
    else:
        print(f"총 {count}개의 즐겨찾기")
```

**핵심 개념**: `show_by_category()`, `search_prompt()`와 동일한 "순회 + 조건 필터링 + 카운팅" 패턴을 반복 적용함으로써, 유사한 구조의 함수를 스스로 응용해 작성하는 역량을 확인하였다.

**4단계 커밋 목록**

| 커밋 해시 | 메시지 |
|---|---|
| `f1685f9` | `feat: implement add_prompt function` |
| `23cb755` | `feat: implement show_list function` |
| `4da7eb3` | `feat: implement show_by_category function` |
| `2d3b2d9` | `feat: implement search_prompt function` |
| `7025874` | `feat: implement show_detail function` |
| `6a0f3ff` | `feat: implement toggle_favorite function` |
| `fad6420` | `feat: implement show_favorites function` |

---

## 5. 브랜치 생성 및 병합 실습

과제 요구사항에 따라, `show_list()` 기능만 별도 브랜치에서 작업 후 `main`에 병합하는 워크플로우를 실습하였다.

```powershell
git checkout -b feature/list      # ① 브랜치 생성 및 이동
# (show_list() 작성 및 테스트)
git add main.py
git commit -m "feat: implement show_list function"
git checkout main                 # ② main으로 복귀
git merge feature/list            # ③ 병합 (Fast-forward)
git push
```

병합은 **Fast-forward 방식**으로 처리되었다. 이는 `main`이 그 사이 별도로 변경되지 않았기 때문에, 병합 커밋을 새로 만들 필요 없이 `main`의 포인터가 `feature/list`의 최신 지점까지 그대로 이동한 것이다.

```
Updating f1685f9..23cb755
Fast-forward
 main.py | 14 +++++++++++++-
```

> 브랜치를 나누어 작업함으로써, 완성되지 않은 코드가 안정적인 `main` 브랜치에 섞이지 않도록 격리하는 원리를 직접 체감하였다.

---

## 6. 예외 처리 (5단계)

과제 요구사항 "잘못된 번호 입력 시 안내 메시지 출력"을 충족하기 위해, 사용자 입력에 의해 발생 가능한 런타임 에러를 방어적으로 처리하였다.

| 위험 지점 | 발생 가능 에러 | 처리 방법 |
|---|---|---|
| `show_detail()`의 번호 입력 | 숫자가 아닌 값 → `ValueError` / 범위 밖 번호 → `IndexError` | `try/except`로 감싸 "잘못된 번호입니다." 출력 후 `return` |
| `show_by_category()`의 번호 입력 | 위와 동일 | 동일 패턴 적용 |
| `toggle_favorite()`의 번호 입력 | 위와 동일 | 동일 패턴 적용 |

**적용 코드 예시**

```python
try:
    index = int(number) - 1
    prompt = prompts[index]
except:
    print("잘못된 번호입니다.")
    return
```

`try/except`는 "실행하기 전에는 성공 여부를 예측하기 어려운 코드"를 안전하게 감싸는 방법으로, "일단 실행해보고, 실패하면 안전망(except)이 받아낸다"는 방어적 프로그래밍의 기본 패턴을 학습하였다. 테스트 결과, 한글(`ㅁㄴ`) 및 범위를 벗어난 숫자(`66`) 입력 모두 프로그램 중단 없이 안전하게 처리됨을 확인하였다.

기존에 이미 구현되어 있던 예외 처리(잘못된 메뉴 번호 → `else` 안내 메시지, 빈 입력값 → `add_prompt()` 내 재입력 요청)와 함께, 이 단계에서 필수 예외 처리 항목이 모두 충족되었다.

**커밋**: `1135086`, `e89811e` (`fix: add exception handling for invalid number input`)

---

## 7. README 작성 및 Git 명령어 완주 (6단계)

### 7-1. README.md 구성

- 프로그램 이름 및 소개
- 실행 방법(`python main.py`)
- 기능 목록 표(1~7번, 0번)
- 프롬프트 카테고리 6종 설명
- 예외 처리 요약
- 개발 환경 정보

### 7-2. 남은 Git 명령어(`clone`, `pull`) 실습

과제 제약 조건에 명시된 8개 명령어(`init, add, commit, push, pull, checkout, clone, merge`)를 전부 사용하기 위해, 아직 실습하지 않았던 두 가지를 별도로 진행하였다.

- **`clone`**: 공개 샘플 저장소(`octocat/Hello-World`)를 클론하여 폴더 구조와 커밋 로그(`git log --oneline`)를 확인 후 정리
- **`pull`**: GitHub 웹 인터페이스에서 `readme.md`를 직접 수정한 뒤, 로컬에서 `git pull`로 반영. Fast-forward 병합으로 처리됨을 확인

이로써 8개 명령어를 모두 최소 1회 이상 사용하는 요구사항을 충족하였다.

**커밋**: `8a40386`(README 작성), `f8cccf7`(웹에서 수정 후 pull로 반영)

---

## 8. 보너스 과제 — JSON 데이터 영속화

### 8-1. 과제 배경

필수 요구사항상 프롬프트 데이터는 "프로그램 실행 중에만 유지, 종료 시 초기화"되도록 되어 있다. 보너스 1번 항목("프롬프트 데이터를 JSON 파일로 저장하고 불러오는 기능")을 구현하여, 프로그램을 종료해도 데이터가 유지되는 영속성(persistence)을 부여하였다.

### 8-2. 설계 방식

자동 저장/불러오기 방식(사용자가 별도 메뉴를 선택할 필요 없이, 프로그램 시작·종료 시점에 자동으로 동작)을 채택하였다.

```
프로그램 시작
   └─ prompts.json 파일이 있으면 그 내용으로 prompts 리스트를 대체
   └─ 파일이 없으면 (최초 실행) 기본 데이터 그대로 사용

프로그램 종료(0번 선택)
   └─ 현재 prompts 리스트 전체를 prompts.json 파일에 저장
```

### 8-3. 구현 코드

**① 모듈 불러오기 (파일 최상단)**

```python
import json
```

**② 프로그램 시작 시 데이터 불러오기**

```python
try:
    with open("prompts.json", "r", encoding="utf-8") as f:
        prompts = json.load(f)
except FileNotFoundError:
    pass
```

**③ 프로그램 종료 시 데이터 저장**

```python
elif choice == "0":
    with open("prompts.json", "w", encoding="utf-8") as f:
        json.dump(prompts, f, ensure_ascii=False, indent=2)
    print("프로그램을 종료합니다.")
    break
```

### 8-4. 핵심 개념 설명

| 요소 | 설명 |
|---|---|
| `open(파일명, "r"/"w", encoding="utf-8")` | 파일을 읽기(`r`) 또는 쓰기(`w`) 모드로 열되, 한글이 깨지지 않도록 UTF-8 인코딩을 명시 |
| `with ... as f:` | 블록이 끝나면 파일을 자동으로 닫아주는 안전한 파일 처리 구문 |
| `json.dump(prompts, f, ensure_ascii=False, indent=2)` | 파이썬 리스트를 JSON 형식으로 변환해 파일에 저장. `ensure_ascii=False`로 한글을 유니코드 이스케이프 없이 그대로 저장, `indent=2`로 가독성 있게 들여쓰기 |
| `json.load(f)` | JSON 파일 내용을 파이썬 리스트/딕셔너리로 복원 |
| `except FileNotFoundError: pass` | 최초 실행 시 `prompts.json`이 존재하지 않아 발생하는 에러를 안전하게 무시하고 기본 데이터를 유지 |

### 8-5. 검증 절차 및 결과

1. 프로그램 실행 → 신규 프롬프트("json") 추가 → 종료
2. `C:\git-test` 폴더에 `prompts.json` 파일이 새로 생성됨을 확인, 한글 데이터가 깨지지 않고 정상 저장됨을 확인
3. 프로그램 재실행 → 목록 조회 시 이전에 추가한 "json" 프롬프트가 그대로 유지됨을 확인
4. 추가 프롬프트("JSON 2개째 추가") 등록 후 재종료·재실행하여 누적 저장까지 검증 → 최종 6개 프롬프트가 모두 정상 유지됨을 확인 (스크린샷 확보)

이로써 "종료 시 초기화"라는 기존 한계를 극복하고, 실제 파일 기반의 데이터 영속화를 구현하였다.

### 8-6. 저장소 정리 — `.gitignore` 적용

`prompts.json`은 실행 중 생성되는 개인 데이터 파일이므로, 소스코드와 구분하여 버전 관리에서 제외하는 것이 바람직하다. 이에 따라 `.gitignore` 파일을 생성하고 아래 규칙을 추가하였다.

```
prompts.json
*.pdf
*.txt
```

이를 통해 실행 결과물(`prompts.json`), 캡처용 PDF, 임시 텍스트 파일이 저장소에 불필요하게 포함되지 않도록 정리하였다.

**커밋**: `cd82e64`(JSON 기능), `da6366c`(.gitignore 추가)

---

## 9. 실전 트러블슈팅 — Push 충돌과 병합 커밋 해결

보너스 기능 커밋 후 `git push`를 시도하는 과정에서, 실무에서 흔히 발생하는 상황을 실제로 경험하고 해결하였다.

### 9-1. 문제 상황

```
! [rejected]        main -> main (fetch first)
hint: Updates were rejected because the remote contains work that you do not
have locally.
```

이는 로컬이 마지막으로 동기화한 시점 이후, GitHub(원격)에 로컬이 알지 못하는 커밋이 추가되어 있어 발생한 상황이다. 즉, 로컬과 원격의 커밋 이력이 서로 어긋나 있어 단순 `push`로는 반영할 수 없는 상태였다.

### 9-2. 해결 절차

```powershell
git pull
```

`pull` 실행 결과, 원격과 로컬의 변경 내용이 자동으로 합쳐지지 않는 지점이 있어 **병합 커밋(merge commit)**이 필요한 상황이 되었고, Git은 기본 편집기(Vim)를 열어 병합 커밋 메시지 입력을 요구하였다.

```
Merge branch 'main' of https://github.com/chalee-victory/git-test
```

Vim에서 기본 제공된 메시지를 그대로 저장(`:wq` 입력 후 Enter)함으로써 병합 커밋을 완료하였고, 이후 `git status`로 "로컬이 원격보다 2커밋 앞서 있음(ahead by 2 commits)"을 확인한 뒤 `git push`를 재시도하여 정상적으로 반영하였다.

### 9-3. 학습적 의의

이 경험을 통해 다음을 체득하였다.

- `push`가 거부되었을 때는 **먼저 `pull`로 원격 상태를 동기화**한 뒤 다시 시도해야 한다는 표준 절차
- `pull`은 내부적으로 `fetch`(원격 정보 가져오기) + `merge`(병합)로 구성되며, 상황에 따라 병합 커밋이 자동 생성될 수 있다는 점
- Git의 기본 커밋 메시지 편집기(Vim)의 최소한의 사용법(`Esc` → `:wq` → `Enter`)

> 이 트러블슈팅은 사전에 계획된 학습 항목은 아니었으나, 여러 환경(로컬 작업 + GitHub 웹 수정)에서 동시에 저장소를 다루는 경우 발생할 수 있는 실제 협업 상황을 미리 경험했다는 점에서 의미가 크다.

---

## 10. 최종 커밋 히스토리

```
da6366c  chore: add .gitignore to exclude json/pdf/txt files
6aed6f3  Merge branch 'main' of https://github.com/chalee-victory/git-test
cd82e64  feat(bonus): add JSON save/load for prompt persistence
f8cccf7  Update readme with project description
8a40386  docs: write README with program description and usage
e89811e  fix: add exception handling for invalid number input
1135086  fix: add exception handling for invalid number input
fad6420  feat: implement show_favorites function
6a0f3ff  feat: implement toggle_favorite function
7025874  feat: implement show_detail function
2d3b2d9  feat: implement search_prompt function
4da7eb3  feat: implement show_by_category function
23cb755  feat: implement show_list function (feature/list)
f1685f9  feat: implement add_prompt function
b23c829  feat: add base prompt data and menu skeleton
dca795b  chore: init project with README
```

**총 커밋 수: 16개** (요구사항 최소 10개 대비 160% 달성)

---

## 11. 요구사항 충족 현황 종합

### 11-1. 필수 요구사항

| 요구사항 | 충족 여부 | 근거 |
|---|---|---|
| 동작하는 콘솔 프로그램 (추가/목록/카테고리별/검색/상세/즐겨찾기 관리/즐겨찾기 목록) | ✅ | 4단계 7개 함수 전체 구현 및 테스트 완료 |
| 프로그램 실행 중 데이터 유지 (기본은 종료 시 초기화) | ✅ | 리스트 기반 관리, 보너스로 영속화까지 확장 |
| 기본 프롬프트 3개 이상 사전 등록 | ✅ | 4개 등록 |
| GitHub 저장소 업로드 | ✅ | `chalee-victory/git-test` |
| 커밋 10개 이상 | ✅ | 16개 |
| 브랜치 생성 및 병합(checkout, merge) 1회 이상 | ✅ | `feature/list` → `main`, 추가로 `pull` 병합도 경험 |
| `init/add/commit/push/pull/checkout/clone/merge` 각 1회 이상 사용 | ✅ | 전부 실습 완료 |
| README.md (설명 + 실행 방법) | ✅ | 6단계에서 작성 |
| 기능별 함수 분리 | ✅ | 7개 함수로 명확히 분리 |
| 잘못된 입력에 대한 예외 처리 | ✅ | 5단계에서 `try/except` 및 조건 검증 적용 |

### 11-2. 보너스 요구사항

| 보너스 항목 | 충족 여부 |
|---|---|
| 보너스 1(a) — JSON 파일 저장/불러오기 | ✅ **구현 완료** |
| 보너스 1(b) — 카테고리별 Markdown 내보내기 | ⬜ 미구현 (선택 사항) |
| 보너스 2 — CRUD(수정/삭제) 및 조회수 기록 | ⬜ 미구현 (선택 사항) |

---

## 12. 제출물 목록

| 항목 | 내용 |
|---|---|
| ① GitHub 저장소 URL | `https://github.com/chalee-victory/git-test` |
| ② 개발환경 설정 스크린샷 | Python 3.12.9, Git 2.55.0.windows.3, 사용자 정보, VSCode-GitHub 연동 |
| ③ 프로그램 실행 결과 스크린샷 | 메뉴 / 프롬프트 추가 / 목록 / 카테고리별 조회 / (보너스) JSON 영속화 결과 |
| ④ `git log --oneline --graph` 결과 스크린샷 | 16개 커밋 및 `feature/list` 브랜치 지점 확인 |

---

## 13. 학습 성과 및 핵심 개념 총정리

### 13-1. Python 문법

| 개념 | 적용 함수 | 설명 |
|---|---|---|
| `enumerate(리스트, start=1)` | show_list, show_by_category, search_prompt, show_favorites | 반복문에서 번호와 항목을 동시에 추출, 사용자 친화적 1부터 시작하는 번호 부여 |
| 조건부 표현식 (`A if 조건 else B`) | 전 함수 공통 | 즐겨찾기 표시(⭐) 처리를 한 줄로 간결화 |
| `in` 연산자 (부분 문자열 포함) | search_prompt | 완전 일치가 아닌 포함 관계 검색 구현 |
| 리스트 인덱싱 (`prompts[index]`) | show_detail, toggle_favorite | 반복 없이 특정 항목 하나에 직접 접근 |
| mutable 자료형과 참조 전달 | toggle_favorite | 딕셔너리를 매개변수로 넘기면 함수 내부 수정이 원본에 반영되는 원리 |
| `try / except` 예외 처리 | show_detail, show_by_category, toggle_favorite | 사용자 입력 오류로 인한 프로그램 강제 종료 방지 |
| `json` 모듈 (`dump`/`load`) | 보너스 기능 | 파이썬 객체와 JSON 파일 간 상호 변환을 통한 데이터 영속화 |

### 13-2. Git / GitHub 워크플로우

| 개념 | 실습 내용 |
|---|---|
| 커밋의 의미 | 단순 저장이 아닌 "의미 있는 작업 단위"의 스냅샷이라는 개념 확립, 기능 단위 커밋 습관화 |
| 브랜치 분리 작업 | `feature/list` 브랜치에서 격리된 개발 후 `main`에 병합, Fast-forward 병합 원리 이해 |
| 원격 저장소 동기화 | `pull`/`push`의 순서 관계, 충돌 발생 시 `pull` 우선 원칙 체득 |
| 병합 커밋 처리 | Vim 편집기를 통한 커밋 메시지 확정 절차 실습 |
| `.gitignore` | 실행 산출물과 임시 파일을 버전 관리에서 제외하는 저장소 정리 원칙 적용 |
| 8대 핵심 명령어 완주 | `init, add, commit, push, pull, checkout, clone, merge` 전체 실습 |

### 13-3. 종합 소감

본 과제를 통해 단순히 "동작하는 프로그램을 완성하는 것"을 넘어, 요구사항을 하나씩 분석하여 함수 단위로 설계하고, 각 기능이 완성될 때마다 의미 있는 커밋으로 기록하며, 예상치 못한 오류(들여쓰기 오류, 변수명 충돌, push 거부 등)를 스스로 진단하고 해결하는 과정을 경험하였다. 특히 보너스 기능(JSON 영속화) 구현과 이 과정에서 발생한 Git 충돌 해결 경험은, 커리큘럼에 명시되지 않았음에도 실무에서 흔히 마주치는 상황을 미리 체득했다는 점에서 의미가 크다.
