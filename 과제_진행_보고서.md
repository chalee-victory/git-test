# Python & Git 기초 과제 진행 보고서
### 나만의 프롬프트 관리 프로그램

**작성자**: 에펠
**GitHub 저장소**: https://github.com/chalee-victory/git-test
**로컬 작업 폴더**: `C:\git-test`
**작성일**: 2026-08-21

---

## 1. 과제 개요

Python과 Git을 처음 배우는 학습자가 "프롬프트 관리 프로그램" 과제를 직접 만들어보며,
Python 기초 문법과 Git/GitHub 워크플로우를 익히는 것을 목표로 진행하였다.

**최종 산출물**
- 콘솔 기반 프롬프트 관리 프로그램 (`main.py`)
- GitHub 저장소 (커밋 10개 이상, 브랜치 생성/병합 이력 포함)

---

## 2. 단계별 진행 내역

### 1단계: 개발 환경 점검 — 완료

| 항목 | 결과 |
|---|---|
| Python 버전 | 3.12.9 |
| Git 버전 | 2.55.0.windows.3 |
| Git 사용자 이름 | 에펠 |
| Git 사용자 이메일 | chalee6110@gmail.com |
| 기본 브랜치명 | main |
| VSCode ↔ GitHub 연동 | chalee-victory 계정 연결 확인 완료 |

---

### 2단계: GitHub 저장소 + 로컬 연결 — 완료

- **GitHub 저장소**: `chalee-victory/git-test`
- **로컬 작업 폴더**: `C:\git-test`
- **원격 연결(origin)**: `https://github.com/chalee-victory/git-test.git`

**진행 중 발생한 이슈와 해결**

1. 예전에 홈 폴더(`C:\Users\OWNER`)에 실수로 `git init`을 해서 `.git` 폴더가 잘못 생성되어 있었음
   → `Remove-Item -Recurse -Force`로 삭제 후 정상 처리 확인

2. `C:\git-test` 폴더에 과제와 무관한 예전 연습 파일들이 섞여 있었음
   → `C:\old-practice-backup` 폴더로 이동 보관

3. `main` 브랜치에 무관한 예전 커밋 4개가 남아있었음
   → `git checkout --orphan clean-main`으로 커밋 이력 없는 새 브랜치 생성
   → `readme.md`만 남기고 나머지는 정리, `chore: init project with README` 커밋 생성
   → `git branch -D main` + `git branch -m main`으로 브랜치 정리
   → `git push -u origin main --force`로 GitHub에도 깨끗한 상태 반영

---

### 3단계: 기본 데이터 구조 + 메뉴 뼈대 작성 — 완료

**작업 파일**: `C:\git-test\main.py`

- `prompts` 리스트에 딕셔너리 4개 등록 (`title`, `content`, `category`, `favorite` 키 구성, 요구사항 "최소 3개" 충족)
- `show_menu()` 함수로 메뉴(1~7번, 0번 종료) 출력
- `while True` 루프 + `input()` 분기 구조로 메뉴 뼈대 작성 (각 메뉴는 placeholder로 시작)
- `python main.py` 실행 테스트 완료

**커밋**
| 커밋 해시 | 메시지 |
|---|---|
| `dca795b` | `chore: init project with README` |
| `b23c829` | `feat: add base prompt data and menu skeleton` |

---

### 4단계: 핵심 기능 함수별 구현 — 완료

7개 함수를 순서대로 구현하고, 완성될 때마다 테스트 후 커밋하였다.

| 순서 | 함수 | 설명 | 비고 |
|---|---|---|---|
| 1 | `add_prompt(prompts)` | 제목/내용/카테고리 입력받아 새 프롬프트 등록. 빈 값 검증 및 Y/N 확인 절차 포함 | `main`에서 작업 |
| 2 | `show_list(prompts)` | 전체 프롬프트를 번호, 카테고리, 즐겨찾기(⭐)와 함께 출력 | **별도 브랜치(`feature/list`)에서 작업 후 `main`에 병합** |
| 3 | `show_by_category(prompts)` | 카테고리 목록 표시 → 선택 → 해당 카테고리 프롬프트만 필터링 출력 | `main`에서 작업 |
| 4 | `search_prompt(prompts)` | 키워드 입력받아 제목/내용에 포함된 프롬프트 검색 | `main`에서 작업 |
| 5 | `show_detail(prompts)` | 번호 입력받아 프롬프트 전체 상세 정보 출력 | `main`에서 작업 |
| 6 | `toggle_favorite(prompts)` | 번호 입력받아 즐겨찾기 상태를 반전(추가/해제) | `main`에서 작업 |
| 7 | `show_favorites(prompts)` | 즐겨찾기(`favorite: True`)된 프롬프트만 모아서 출력 | `main`에서 작업 |

**브랜치 작업 상세 (show_list 기능)**

```powershell
git checkout -b feature/list      # 브랜치 생성 및 이동
# (show_list() 작성 및 테스트)
git add main.py
git commit -m "feat: implement show_list function"
git checkout main                 # main으로 복귀
git merge feature/list            # Fast-forward 병합
git push
```

**주요 학습 개념**
- `enumerate(리스트, start=1)`: 반복문에서 번호와 항목을 함께 꺼내는 방법, `start=1`은 사용자 친화적 번호 표시를 위함
- 조건부 표현식(`"⭐" if 조건 else ""`): 즐겨찾기 표시를 한 줄로 처리
- 딕셔너리 값 직접 수정(`prompt["favorite"] = not prompt["favorite"]`): 리스트/딕셔너리는 mutable(변경 가능) 자료형이라, 매개변수로 넘긴 원본이 함수 내부 수정에 의해 실제로 바뀜(참조 전달 원리)
- 문자열 포함 검사(`in` 연산자): `search_prompt()`에서 제목/내용 부분 일치 검색에 활용

**커밋**
| 커밋 해시 | 메시지 |
|---|---|
| `f1685f9` | `feat: implement add_prompt function` |
| `23cb755` | `feat: implement show_list function` (feature/list) |
| `4da7eb3` | `feat: implement show_by_category function` |
| `2d3b2d9` | `feat: implement search_prompt function` |
| `7025874` | `feat: implement show_detail function` |
| `6a0f3ff` | `feat: implement toggle_favorite function` |
| `fad6420` | `feat: implement show_favorites function` |

---

### 5단계: 예외 처리 — 완료

과제 요구사항에 따라 아래 항목에 대한 예외 처리를 적용하였다.

| 항목 | 처리 내용 |
|---|---|
| 잘못된 메뉴 번호 | `else:` 분기에서 안내 메시지 출력 후 메뉴로 복귀 |
| 빈 입력값 (제목/내용/카테고리) | `add_prompt()`에서 빈 문자열 검증 (제목은 취소, 내용/카테고리는 재입력 요청) |
| 존재하지 않는 프롬프트 번호 / 숫자가 아닌 입력 | `show_detail()`, `show_by_category()`, `toggle_favorite()`에 `try / except` 적용 |
| 종료(0번) 처리 | `break`로 정상 종료 확인 |

**적용 예시 (`show_detail` 함수)**

```python
try:
    index = int(number) - 1
    prompt = prompts[index]
except:
    print("잘못된 번호입니다.")
    return
```

동일한 패턴을 `show_by_category()`, `toggle_favorite()`에도 적용하여, 문자열 입력이나 범위를 벗어난 번호 입력 시에도 프로그램이 종료되지 않고 안전하게 메뉴로 복귀하도록 처리하였다.

**커밋**
| 커밋 해시 | 메시지 |
|---|---|
| `1135086`, `e89811e` | `fix: add exception handling for invalid number input` |

---

### 6단계: README 작성 및 최종 정리 — 완료

`readme.md`에 아래 내용을 작성하였다.

- 프로그램 이름 및 소개
- 실행 방법 (`python main.py`)
- 기능 목록 표 (1~7번, 0번)
- 프롬프트 카테고리 6종 설명
- 예외 처리 내용 요약
- 개발 환경 (Python 3.12.9, Git 2.55.0)

**Git 명령어 실습 보완**

과제 제약 조건에 명시된 8개 명령어(`init, add, commit, push, pull, checkout, clone, merge`)를 모두 사용하기 위해, 아직 사용하지 않았던 `clone`과 `pull`을 추가로 실습하였다.

- **clone**: 공개 샘플 저장소(`octocat/Hello-World`)를 클론하여 구조 및 커밋 로그 확인 후 삭제
- **pull**: GitHub 웹에서 `readme.md`를 직접 수정한 뒤, 로컬에서 `git pull`로 반영 (Fast-forward 병합 확인)

**커밋**
| 커밋 해시 | 메시지 |
|---|---|
| `8a40386` | `docs: write README with program description and usage` |
| `f8cccf7` | `Update readme with project description` (GitHub 웹 수정분, pull로 반영) |

---

## 3. 최종 커밋 히스토리

```
f8cccf7 (HEAD -> main, origin/main, origin/HEAD) Update readme with project description
8a40386 docs: write README with program description and usage
e89811e fix: add exception handling for invalid number input
1135086 fix: add exception handling for invalid number input
fad6420 feat: implement show_favorites function
6a0f3ff feat: implement toggle_favorite function
7025874 feat: implement show_detail function
2d3b2d9 feat: implement search_prompt function
4da7eb3 feat: implement show_by_category function
23cb755 (feature/list) feat: implement show_list function
f1685f9 feat: implement add_prompt function
b23c829 feat: add base prompt data and menu skeleton
dca795b chore: init project with README
```

**총 커밋 수: 13개**

---

## 4. 요구사항 충족 현황

| 요구사항 | 충족 여부 | 비고 |
|---|---|---|
| 동작하는 콘솔 프로그램 (7개 기능) | ✅ | 추가/목록/카테고리별/검색/상세/즐겨찾기 관리/즐겨찾기 목록 |
| 기본 프롬프트 3개 이상 | ✅ | 기본 4개 등록 |
| 프로그램 실행 중 데이터 유지 (종료 시 초기화) | ✅ | 리스트 기반, 별도 저장 없음 |
| GitHub 저장소 업로드 | ✅ | `chalee-victory/git-test` |
| 커밋 10개 이상 | ✅ | 13개 |
| 브랜치 생성 및 병합 1회 이상 | ✅ | `feature/list` → `main` |
| `init/add/commit/push/pull/checkout/clone/merge` 각 1회 이상 | ✅ | 전부 사용 확인 |
| README.md (설명 + 실행 방법) | ✅ | 작성 완료 |
| 기능별 함수 분리 | ✅ | 7개 함수로 분리 |

---

## 5. 향후 남은 작업 (제출 준비)

| 제출물 | 상태 |
|---|---|
| GitHub 저장소 URL | ✅ 준비 완료 |
| 개발환경 설정 스크린샷 | 준비 필요 (VSCode, Python/Git 버전, git config) |
| 프로그램 실행 결과 스크린샷 (메뉴/추가/목록/검색 등) | 준비 필요 |
| `git log --oneline --graph` 스크린샷 | ✅ 확보 완료 |

---

## 6. 학습 소감 및 주요 개념 정리

- **Git 커밋**은 단순 파일 저장이 아니라 "의미 있는 작업 단위"의 버전 스냅샷이라는 점을 체감함
- **브랜치/병합**: `main`(안정 버전)과 `feature/list`(작업 공간)를 분리함으로써, 미완성 코드가 안정 버전에 섞이지 않도록 하는 원리를 실습함
- **파이썬 리스트/딕셔너리의 참조 전달**: 함수에 매개변수로 넘긴 리스트/딕셔너리를 함수 내부에서 수정하면 원본도 함께 바뀐다는 mutable 자료형의 특성을 `toggle_favorite()` 구현을 통해 이해함
- **예외 처리(`try/except`)**: 사용자의 잘못된 입력(숫자가 아닌 값, 범위를 벗어난 번호)으로 프로그램이 강제 종료되지 않도록 방어적으로 코드를 작성하는 습관을 익힘
